package dev.wuason.unearthMechanic.compatibilities.craftengine.types

enum class BehaviorProperties {
}

enum class WindowTile {
    single,
    left, middle, right,
    up_left, up, up_right,
    down_left, down, down_right
}

enum class SofaTile {
    single,
    left, middle, right
}

enum class ColumnPosition {
    single,
    up, middle, down
}

enum class FishType {
    none,
    cod,
    salmon,
    tropical_fish,
    pufferfish,
    axolotl,
    tadpole
}

enum class CurtainYPos {
    single,
    bottom,
    middle,
    top
}